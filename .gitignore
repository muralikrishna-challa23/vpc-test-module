# Terraform generated files
.terraform/
*.tfstate
*.tfstate.*
.terraformrc
terraform.rc

# Crash log files
crash.log
crash.*.log

# Variable files (potentially containing sensitive data)
*.tfvars
*.tfvars.json

# Override files (often used for local development and not meant for version control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan output (if you generate and save plans)
*.tfplan*

# Editor/IDE specific files (example for VS Code)
.vscode/
.idea/